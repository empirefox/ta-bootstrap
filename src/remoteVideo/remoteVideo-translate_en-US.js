'use strict';

angular.module('ta.bootstrap.remoteVideo').config(['$translateProvider',
function($translateProvider) {
	$translateProvider.translations('en-US', {
		TA_RV_TOOLTIP : 'Insert video'
	});
}]);