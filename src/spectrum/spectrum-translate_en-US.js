'use strict';

angular.module('ta.bootstrap.spectrum').config(['$translateProvider',
function($translateProvider) {
	$translateProvider.translations('en-US', {
		TA_SPECTRUM_TOOLTIP : 'Change font color'
	});
}]);