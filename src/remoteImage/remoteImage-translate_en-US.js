'use strict';

angular.module('ta.bootstrap.remoteImage').config(['$translateProvider',
function($translateProvider) {
	$translateProvider.translations('en-US', {
		TA_RI_TOOLTIP : 'Insert image'
	});
}]);